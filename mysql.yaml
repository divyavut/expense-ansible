- name: configure mysql 
  hosts: mysql 
  become: yes
  tasks: 
  - name: install mysql-server
    ansible.builtin.package:
      name: mysql-server
      state: present 
  - name: install mysql-server
    ansible.builtin.service:
      name: mysql-server
      state: started 
      enabled: yes
